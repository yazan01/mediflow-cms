# MediFlow CMS — وثيقة النظام الشاملة ودليل المستخدم

> **الإصدار:** 2.0 | **تاريخ الإصدار:** مايو 2026 | **النظام:** MediFlow CMS

---

## جدول المحتويات

1. [نظرة عامة على النظام](#1-نظرة-عامة-على-النظام)
2. [المعمارية التقنية](#2-المعمارية-التقنية)
3. [نظام الصلاحيات والأدوار](#3-نظام-الصلاحيات-والأدوار)
4. [دليل التثبيت والتشغيل](#4-دليل-التثبيت-والتشغيل)
5. [دليل المستخدم — وحدة بوحدة](#5-دليل-المستخدم--وحدة-بوحدة)
   - [5.1 تسجيل الدخول والجلسة](#51-تسجيل-الدخول-والجلسة)
   - [5.2 لوحة التحكم الرئيسية](#52-لوحة-التحكم-الرئيسية)
   - [5.3 إدارة المرضى](#53-إدارة-المرضى)
   - [5.4 المواعيد](#54-المواعيد)
   - [5.5 السجل الطبي الإلكتروني (EMR)](#55-السجل-الطبي-الإلكتروني-emr)
   - [5.6 الفواتير والمدفوعات](#56-الفواتير-والمدفوعات)
   - [5.7 الصيدلية](#57-الصيدلية)
   - [5.8 المختبر](#58-المختبر)
   - [5.9 الأشعة](#59-الأشعة)
   - [5.10 الموارد البشرية](#510-الموارد-البشرية)
   - [5.11 المحاسبة](#511-المحاسبة)
   - [5.12 التقارير](#512-التقارير)
   - [5.13 إدارة المستخدمين](#513-إدارة-المستخدمين)
   - [5.14 الإعدادات](#514-الإعدادات)
   - [5.15 سجل التدقيق](#515-سجل-التدقيق)
   - [5.16 البحث الشامل](#516-البحث-الشامل)
   - [5.17 اختصارات لوحة المفاتيح](#517-اختصارات-لوحة-المفاتيح)
   - [5.18 مؤشر حالة الاتصال](#518-مؤشر-حالة-الاتصال)
6. [مرجع API الكامل](#6-مرجع-api-الكامل)
7. [الأمان والحماية](#7-الأمان-والحماية)
8. [إدارة قاعدة البيانات](#8-إدارة-قاعدة-البيانات)
9. [استكشاف الأخطاء وحلها](#9-استكشاف-الأخطاء-وحلها)
10. [مرجع سريع للمشرف](#10-مرجع-سريع-للمشرف)

---

## 1. نظرة عامة على النظام

**MediFlow CMS** نظام إدارة عيادات متكامل (Clinic ERP) مصمم لتغطية جميع العمليات التشغيلية في المنشآت الصحية — من استقبال المريض إلى إصدار الفاتورة وإدارة المخزون وتقارير الأداء.

### الوحدات الرئيسية

| الوحدة | الغرض | المستخدمون المستهدفون |
|---|---|---|
| إدارة المرضى | تسجيل وإدارة ملفات المرضى | الاستقبال، الممرضون، الأطباء |
| المواعيد | جدولة وإدارة مواعيد الزيارات | الاستقبال، الأطباء |
| السجل الطبي الإلكتروني | الاستشارات، العلامات الحيوية، تشخيص الأمراض | الأطباء، الممرضون |
| الفواتير | إصدار الفواتير وتسجيل المدفوعات | المحاسبون، الاستقبال |
| الصيدلية | مخزون الأدوية وصرف الوصفات | الصيادلة |
| المختبر | طلبات الفحوصات ونتائجها | فنيو المختبر |
| الأشعة | طلبات الأشعة وتقاريرها | أخصائيو الأشعة |
| الموارد البشرية | الموظفون، الحضور، الإجازات، الرواتب | مسؤولو الموارد البشرية |
| المحاسبة | الأصول، المصروفات، الموردون | المحاسبون |
| التقارير | إحصاءات الأداء والإيرادات | المديرون |
| إدارة المستخدمين | صلاحيات الوصول وإدارة الحسابات | المشرف العام |
| الإعدادات | إعدادات النظام والفروع والمناوبات | المشرف العام |
| سجل التدقيق | تتبع جميع العمليات | المدققون، المشرف العام |

---

## 2. المعمارية التقنية

### مخطط المكونات

**بيئة التطوير (Windows):**
```
المتصفح :3000 → Next.js → /api/* proxy rewrite → FastAPI :8000 → MySQL
```

**بيئة الإنتاج (Ubuntu):**
```
┌─────────────────────────────────────────────────────────────┐
│                    المتصفح (HTTPS :443)                      │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              Nginx — Reverse Proxy                            │
│   /api/*  ──────────────────────────► FastAPI :8000          │
│   /       ──────────────────────────► Next.js :3000          │
│   /_next/static/ (cache immutable)                           │
│   HTTP → HTTPS redirect تلقائي                              │
│   SSL/TLS — Let's Encrypt (Certbot)                          │
└───────┬─────────────────────────────────┬────────────────────┘
        │                                 │
        ▼                                 ▼
┌──────────────────┐             ┌─────────────────────────────┐
│ Next.js :3000    │             │ FastAPI :8000                │
│ middleware.ts    │             │ routers + SQLAlchemy ORM     │
│ JWT Edge Auth    │             │ TokenBlocklist + Alembic     │
│ صفحات النظام    │             └───────────┬─────────────────┘
└──────────────────┘                         │
                                             ▼
                                   ┌─────────────────┐
                                   │ MySQL 8.4 :3306  │
                                   └─────────────────┘
```

### تدفق المصادقة (Auth Flow)

```
1. المستخدم يُدخل بريده وكلمة مروره
2. POST /api/auth/login → FastAPI يتحقق بـ bcrypt
3. FastAPI يُصدر JWT token ويضعه في cookie (HttpOnly, SameSite=Strict, 8h)
4. middleware.ts يتحقق من الكوكي في كل طلب (Edge Runtime)
5. layout.tsx يقرأ الكوكي من الخادم لعرض بيانات المستخدم
6. عند تسجيل الخروج: JWT يُضاف إلى TokenBlocklist في قاعدة البيانات
```

### مكدس التقنيات

| الطبقة | التقنية | الإصدار |
|---|---|---|
| Frontend | Next.js App Router + TypeScript | 16 |
| Backend | FastAPI + Python | 3.11+ |
| ORM | SQLAlchemy | 2.0 |
| Migrations | Alembic | 1.14 |
| قاعدة البيانات | MySQL | 8.4 |
| المصادقة | JWT (python-jose) + bcrypt (passlib) | — |
| UI | Tailwind CSS v4 + Material Symbols | 4 |
| الرسوم البيانية | Recharts | — |
| Rate Limiting | slowapi | — |
| Edge JWT | jose | — |
| State Management | Zustand | — |
| Tables | TanStack React Table | — |
| Real-time | Server-Sent Events (SSE) | — |
| i18n | Arabic / English (LanguageContext) | — |
| Timezone | Intl.DateTimeFormat (TimezoneContext) | — |

---

## 3. نظام الصلاحيات والأدوار

### قائمة الأدوار

| الدور | الرمز | الوصف |
|---|---|---|
| المشرف العام | `SUPER_ADMIN` | وصول كامل لجميع الوحدات |
| مدير العيادة | `CLINIC_MANAGER` | إدارة شاملة بدون حذف المستخدمين |
| طبيب | `DOCTOR` | EMR، مواعيده، وصفاته |
| ممرض/ة | `NURSE` | EMR، المرضى، المواعيد |
| موظف استقبال | `RECEPTIONIST` | المرضى، المواعيد، الفواتير |
| صيدلاني | `PHARMACIST` | مخزون الأدوية وصرف الوصفات |
| فني مختبر | `LAB_TECHNICIAN` | طلبات المختبر ونتائجها |
| أخصائي أشعة | `RADIOLOGIST` | طلبات الأشعة وتقاريرها |
| محاسب | `ACCOUNTANT` | الفواتير، المحاسبة، التقارير |
| مسؤول موارد بشرية | `HR_OFFICER` | الموظفون، الحضور، الإجازات، الرواتب |
| مدقق | `AUDITOR` | عرض سجل التدقيق فقط |
| موظف | `STAFF` | وصول محدود |

### مصفوفة الصلاحيات

| الوحدة | SUPER_ADMIN | CLINIC_MANAGER | DOCTOR | NURSE | RECEPTIONIST | PHARMACIST | LAB_TECH | RADIOLOGIST | ACCOUNTANT | HR_OFFICER | AUDITOR |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| لوحة التحكم | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| المرضى (قراءة) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ |
| المرضى (تعديل) | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| المرضى (حذف) | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| المواعيد | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| EMR | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| الفواتير | ✅ | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ |
| الصيدلية | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| المختبر | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| الأشعة | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ |
| الموارد البشرية | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| المحاسبة | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ |
| التقارير | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ |
| المستخدمون | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| الإعدادات | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| التدقيق | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |

---

## 4. دليل التثبيت والتشغيل

### المتطلبات الأساسية

| البرنامج | الإصدار الأدنى |
|---|---|
| Python | 3.11 |
| Node.js | 20 |
| npm | 10 |
| MySQL | 8.4 |

### التثبيت على Windows (الطريقة السريعة)

```cmd
install.bat
```

يُنفّذ هذا الملف خطوات التثبيت كاملة:
1. يتحقق من إصدارات Python وNode.js
2. ينشئ Python virtual environment في `backend/.venv`
3. يثبت جميع مكتبات Python (`pip install -r requirements.txt`)
4. يثبت مكتبات Node.js (`npm install`)
5. يسألك عن بيانات MySQL (المضيف، المستخدم، كلمة المرور)
6. ينشئ قاعدة البيانات `mediflow` والجداول كاملة
7. يُضيف المستخدم الافتراضي (admin)
8. يُولّد ملف `backend/.env` تلقائياً

**بيانات الدخول الافتراضية:**
```
الرابط:     http://localhost:3000
البريد:     admin@mediflow.com
كلمة المرور: Admin@1234
```

> **تحذير أمني:** غيّر كلمة المرور الافتراضية فور أول تسجيل دخول.

### التشغيل اليومي

```cmd
start.bat   # يشغّل MySQL + Backend + Frontend ويفتح المتصفح
stop.bat    # يوقف جميع الخدمات
```

### التشغيل اليدوي (للتطوير)

**Terminal 1 — Backend:**
```cmd
cd backend
python -m uvicorn main:app --reload --port 8000
```

**Terminal 2 — Frontend:**
```cmd
npm run dev
```

> **ملاحظة:** استخدم `cmd` وليس `PowerShell` لتجنب مشاكل الصلاحيات مع npm.

### التثبيت على Ubuntu Server (الإنتاج)

#### الطريقة السريعة — نشر كامل تلقائي بأمر واحد

```bash
git clone https://github.com/YOUR_USERNAME/mediflow-cms.git /opt/mediflow
sudo bash /opt/mediflow/deploy/deploy.sh
```

السكريبت يسألك **أربعة أسئلة فقط في البداية** ثم يُكمل كل شيء وحده:

| السؤال | الوصف |
|---|---|
| رابط المستودع | عنوان Git repo |
| النطاق أو IP | لـ Nginx — IP يعني HTTP فقط، نطاق يعني جاهز لـ HTTPS |
| كلمة مرور MySQL | إذا مثبّت → يطلب الحالية ويتحقق منها؛ إذا غير مثبّت → يثبّته ويطلب تعيين جديدة |
| كلمة مرور System Admin | لحساب `admin@mediflow.com` مع تحقق من القوة والتأكيد |

#### ما يفعله السكريبت تلقائياً

1. تثبيت Python 3.11، Node.js 20، Nginx، MySQL (إذا لزم)
2. إنشاء مستخدم نظام `mediflow` ومستخدم قاعدة بيانات مخصص
3. توليد `JWT_SECRET` و`SETTINGS_ENCRYPTION_KEY` وكلمة مرور DB تلقائياً
4. كتابة ملفات `.env` بالقيم المولّدة — **لا حاجة لتعديل يدوي**
5. إنشاء جداول قاعدة البيانات وتطبيق الـ migrations
6. تهيئة حساب System Admin بالكلمة التي أدخلتها
7. بناء Next.js وتشغيل خدمات systemd
8. إعداد Nginx — HTTP فقط للـ IP، أو نطاق جاهز لـ HTTPS

#### تفعيل HTTPS (للنطاقات فقط)

```bash
sudo certbot --nginx -d yourdomain.com
```

#### تحديث النظام بعد كل push

```bash
sudo bash /opt/mediflow/deploy/update.sh
```

يقوم تلقائياً بـ: `git pull` → تحديث مكتبات Python → تطبيق migrations → بناء Next.js → إعادة تشغيل الخدمات.

### إعادة تعيين قاعدة البيانات

```cmd
cd backend
python reset_db.py --confirm
```

> **تحذير:** يحذف جميع البيانات نهائياً. لديك 3 ثوانٍ للإلغاء بـ `Ctrl+C`.

---

## 5. دليل المستخدم — وحدة بوحدة

### 5.1 تسجيل الدخول والجلسة

#### الدخول إلى النظام

1. افتح المتصفح وانتقل إلى `http://localhost:3000`
2. أدخل **البريد الإلكتروني** وكلمة المرور
3. اضغط **دخول** أو `Enter`

**ملاحظات:**
- الحساب يُقفل تلقائياً بعد **10 محاولات فاشلة** في الدقيقة (rate limiting)
- كلمة المرور تُشفَّر بـ bcrypt — لا يستطيع أحد الاطلاع عليها

#### إدارة الجلسة

- **مدة الجلسة:** 8 ساعات من وقت الدخول
- **انتهاء بسبب الخمول:** بعد **25 دقيقة** من عدم النشاط تظهر رسالة تحذيرية
- **تسجيل خروج تلقائي:** إذا لم تتفاعل بعد 5 دقائق من التحذير
- **تغيير اللغة:** انقر على العلم في الشريط العلوي للتبديل بين العربية والإنجليزية

#### تسجيل الخروج

- انقر على اسمك في الشريط العلوي الأيمن
- اختر **تسجيل الخروج**
- سيُبطَل الـ token فوراً ولا يمكن إعادة استخدامه حتى لو سُرق

---

### 5.2 لوحة التحكم الرئيسية

#### ما تعرضه اللوحة

- **إجمالي المرضى** — عدد المرضى المسجلين في النظام
- **مواعيد اليوم** — عدد المواعيد المجدولة لليوم
- **إيرادات هذا الشهر** — مجموع المدفوعات في الشهر الحالي
- **الأدوية المنخفضة** — عدد الأدوية التي وصلت حد التنبيه
- **رسم بياني للمواعيد** — توزيع المواعيد على مدار الأسبوع
- **آخر المواعيد** — قائمة المواعيد الأخيرة مع حالتها

#### كيفية الاستخدام

لا يتطلب أي إدخال — تُحدَّث البيانات تلقائياً عند كل زيارة للصفحة.

#### تحديثات فورية (Real-time)

تدعم لوحة التحكم تحديثات مباشرة عبر **Server-Sent Events (SSE)**:
- مؤشر أخضر متحرك يظهر بجانب عنوان الصفحة عند الاتصال الحي
- تُحدَّث بطاقات KPI (المرضى الجدد، مواعيد اليوم، المواعيد الحالية) لحظياً دون إعادة تحميل الصفحة
- عند انقطاع الاتصال بالخادم يتحول المؤشر إلى أصفر ويُعاد الاتصال تلقائياً بفترات متصاعدة (2s، 4s، 8s... حتى 30s)

---

### 5.3 إدارة المرضى

#### عرض قائمة المرضى

1. اضغط على **المرضى** في القائمة الجانبية
2. تظهر القائمة مع إمكانية:
   - **البحث:** بالاسم، الرقم الطبي (MRN)، رقم الهاتف، الرقم الوطني، البريد
   - **التصفية:** نشط / غير نشط / الكل
   - **الترقيم:** 10 مرضى في الصفحة (قابل للتغيير)

#### إضافة مريض جديد

1. اضغط **+ إضافة مريض**
2. أدخل البيانات الإلزامية:
   - الاسم الأول والأخير
   - تاريخ الميلاد
   - الجنس (ذكر / أنثى)
   - رقم الهاتف
3. البيانات الاختيارية:
   - الجنسية والرقم الوطني
   - البريد الإلكتروني
   - العنوان
   - فصيلة الدم (`A+`, `A-`, `B+`, `B-`, `AB+`, `AB-`, `O+`, `O-`)
   - الحساسيات (قائمة — أدخل كل حساسية واضغط Enter)
   - الأمراض المزمنة (قائمة)
   - جهة الاتصال في حالات الطوارئ (الاسم والهاتف)
   - معلومات التأمين (المزود، رقم الوثيقة، نوع التغطية، تاريخ الانتهاء)
   - ملاحظات
4. اضغط **حفظ**

> **الرقم الطبي (MRN):** يُولَّد تلقائياً ولا يمكن تغييره.

#### عرض ملف المريض

اضغط على اسم المريض لفتح ملفه الكامل، الذي يتضمن:
- **البيانات الشخصية** — المعلومات الأساسية والتأمين
- **سجل المواعيد** — جميع مواعيده السابقة
- **سجل الاستشارات** — ملخص كل زيارة
- **الفواتير** — سجل الفواتير والمدفوعات

#### تعديل بيانات المريض

1. افتح ملف المريض
2. اضغط **تعديل**
3. عدّل البيانات المطلوبة
4. اضغط **حفظ**

#### حذف مريض (Soft Delete)

- الحذف **ليس نهائياً** — يُوضَع تاريخ الحذف (`deletedAt`) والمريض يختفي من القوائم
- لا يُحذف أي سجل طبي أو مالي مرتبط بالمريض
- متاح لـ `SUPER_ADMIN` و `CLINIC_MANAGER` فقط

---

### 5.4 المواعيد

#### عرض المواعيد

- **عرض القائمة:** جدول بجميع المواعيد مع إمكانية البحث والتصفية
- **عرض التقويم:** رؤية مرئية بالأيام
- **التصفية:** حسب الحالة (مجدول، مكتمل، ملغى، لم يحضر)
- **التصفية حسب الطبيب والتاريخ**

#### حالات الموعد

| الحالة | المعنى |
|---|---|
| `SCHEDULED` | مجدول — في الانتظار |
| `CONFIRMED` | مؤكد من المريض أو الاستقبال |
| `IN_PROGRESS` | جارٍ — المريض عند الطبيب |
| `COMPLETED` | انتهى بنجاح |
| `CANCELLED` | تم الإلغاء |
| `NO_SHOW` | لم يحضر المريض |
| `RESCHEDULED` | أُعيد جدولته |

#### حجز موعد جديد

1. اضغط **+ موعد جديد**
2. اختر المريض (ابحث بالاسم أو MRN)
3. اختر الطبيب
4. حدد التاريخ والوقت
5. اختر نوع الموعد:
   - **REGULAR** — زيارة عادية
   - **FOLLOW_UP** — متابعة
   - **EMERGENCY** — طارئ
   - **CONSULTATION** — استشارة
   - **PROCEDURE** — إجراء طبي
6. أدخل سبب الزيارة (اختياري)
7. اضغط **حجز**

#### إدارة الموعد

- **تغيير الحالة:** اضغط على الموعد ثم عدّل حالته
- **تحديث ملاحظات الحضور** — سبب الإلغاء، الوزن، الضغط، إلخ
- **إلغاء الموعد** — مع ذكر السبب

#### الإجراءات السريعة (Quick Actions)

يوفر جدول المواعيد عمود **إجراءات سريعة** لكل موعد:

| الزر | الوظيفة |
|---|---|
| **فتح EMR** | ينتقل مباشرة إلى السجل الطبي الإلكتروني للمريض |
| **استشارة سريعة** | يفتح نافذة إدخال الاستشارة دون مغادرة صفحة المواعيد |
| **إصدار فاتورة** | ينتقل إلى صفحة الفواتير مع ربط الموعد تلقائياً |

**الاستشارة السريعة:**
1. اضغط أيقونة **الاستشارة السريعة** في عمود الإجراءات
2. تظهر نافذة تحتوي على:
   - الشكوى الرئيسية (Chief Complaint)
   - التقييم والتشخيص (Assessment)
   - خطة العلاج (Plan)
3. اضغط **حفظ** — تُحفظ الاستشارة ويتغير حالة الموعد إلى `IN_CONSULTATION`

---

### 5.5 السجل الطبي الإلكتروني (EMR)

#### الوصول إلى EMR المريض

**من قائمة المرضى:**
- افتح ملف المريض ← اضغط **السجل الطبي**

**من قائمة EMR:**
1. اضغط **EMR** في القائمة الجانبية
2. ابحث عن المريض
3. اضغط على اسمه

#### محتوى صفحة EMR

**ملخص المريض:**
- الحساسيات والأمراض المزمنة
- الأدوية الحالية
- آخر العلامات الحيوية

**تبويب الاستشارات:**
- كل استشارة تحتوي على:
  - الشكوى الرئيسية (Chief Complaint)
  - الجانب الذاتي (Subjective)
  - الفحص السريري (Objective)
  - التقييم والتشخيص (Assessment)
  - خطة العلاج (Plan)
  - الأدوية الموصوفة
  - الحالة: مفتوحة / مُقفَّلة

**تبويب نتائج المختبر:**
- نتائج جميع الفحوصات المختبرية مرتبة زمنياً

**تبويب نتائج الأشعة:**
- تقارير الأشعة مع الاستنتاجات

#### إضافة استشارة جديدة

1. في صفحة EMR ← تبويب **الاستشارات**
2. اضغط **+ استشارة جديدة**
3. أدخل:
   - الشكوى الرئيسية
   - التاريخ والأعراض (Subjective)
   - نتائج الفحص (Objective)
   - التشخيص (Assessment)
   - خطة العلاج (Plan)
4. أضف الأدوية الموصوفة إن وجدت
5. اضغط **حفظ**

**قفل الاستشارة:** بعد الانتهاء، اضغط **قفل** — الاستشارة المُقفَّلة لا يمكن تعديلها للحفاظ على سلامة السجل الطبي.

---

### 5.6 الفواتير والمدفوعات

#### عرض الفواتير

- **التصفية حسب الحالة:** معلق، جزئي، مكتمل، متأخر، ملغى، مُسترجَع
- **البحث:** برقم الفاتورة أو اسم المريض
- **التصفية بالتاريخ:** من تاريخ — إلى تاريخ

#### حالات الفاتورة

| الحالة | المعنى |
|---|---|
| `PENDING` | معلقة — لم يُدفع شيء |
| `PARTIAL` | مدفوع جزئياً |
| `PAID` | مدفوعة بالكامل |
| `OVERDUE` | متأخرة عن موعد الاستحقاق |
| `CANCELLED` | ملغاة |
| `REFUNDED` | مُسترجَعة |

#### إنشاء فاتورة جديدة

1. اضغط **+ فاتورة جديدة**
2. اختر المريض
3. اختر الموعد (اختياري — لربط الفاتورة بموعد)
4. أضف بنود الفاتورة:
   - **الوصف** (مثال: استشارة طبيب)
   - **الفئة** (استشارة، إجراء، دواء، مختبر، أشعة)
   - **الكمية**
   - **سعر الوحدة**
   - **الخصم %** (اختياري لكل بند)
5. حدد (اختياري):
   - **إجمالي الخصم**
   - **نسبة الضريبة**
6. معلومات التأمين (إن كانت الفاتورة مطالبة تأمين):
   - ضع علامة **مطالبة تأمين**
   - أدخل اسم المزود ورقم الوثيقة
7. اضغط **إنشاء**

#### تسجيل دفعة

1. افتح الفاتورة
2. اضغط **+ تسجيل دفعة**
3. أدخل:
   - **المبلغ** (لا يمكن تجاوز الرصيد المتبقي)
   - **طريقة الدفع:** نقداً، بطاقة، شيك، تحويل، تأمين
   - **رقم المرجع** (اختياري — مفيد للشيكات والتحويلات)
   - **ملاحظات** (اختياري)
4. اضغط **حفظ**

> **الدفع بالأقساط:** يمكن تسجيل دفعات متعددة على نفس الفاتورة. تتغير الحالة تلقائياً إلى `PARTIAL` ثم `PAID` عند سداد المبلغ كاملاً.

#### إحصاءات الفوترة

تظهر في أعلى الصفحة:
- **إجمالي الإيرادات** — مجموع كل المدفوعات
- **معلق** — مجموع الفواتير غير المسددة
- **متأخر** — مجموع الفواتير المتجاوزة تاريخ الاستحقاق
- **مُحصَّل هذا الشهر** — مدفوعات الشهر الحالي

---

### 5.7 الصيدلية

#### عرض المخزون

- قائمة بجميع الأدوية مع:
  - الاسم التجاري والاسم العلمي
  - الكمية المتوفرة
  - الكمية الدنيا (حد التنبيه)
  - تاريخ انتهاء الصلاحية
  - سعر الوحدة

#### إضافة دواء جديد

1. اضغط **+ دواء جديد**
2. أدخل:
   - الاسم التجاري
   - الاسم العلمي (اختياري)
   - الوحدة (حبة، مل، جرعة، إلخ)
   - الكمية الحالية
   - الكمية الدنيا (للتنبيه عند الانخفاض)
   - سعر الوحدة
   - تاريخ انتهاء الصلاحية
3. اضغط **حفظ**

#### تعديل المخزون (حركات المخزون)

1. افتح تفاصيل الدواء
2. اضغط **+ حركة مخزون**
3. اختر النوع:
   - **إضافة (IN):** استلام شحنة، مرتجع
   - **صرف (OUT):** صرف وصفة، هدر
4. أدخل الكمية والسبب
5. اضغط **حفظ**

> تُسجَّل جميع حركات المخزون مع بيانات المستخدم والتاريخ للتدقيق.

#### تنبيهات المخزون المنخفض

تظهر الأدوية المنخفضة (كميتها أقل من الحد الأدنى) في لوحة التحكم وفي قمة صفحة الصيدلية.

---

### 5.8 المختبر

#### إنشاء طلب مختبر

1. انتقل إلى صفحة **EMR** للمريض
2. في قسم طلبات المختبر، اضغط **+ طلب جديد**
3. أدخل:
   - الفحوصات المطلوبة (يمكن إضافة أكثر من فحص)
   - الأولوية (عادية / عاجلة)
   - ملاحظات للفني
4. اضغط **إرسال**

#### إدخال النتائج

1. افتح صفحة **المختبر**
2. ابحث عن الطلب أو اختره من القائمة
3. اضغط **إدخال نتائج**
4. أدخل قيمة كل فحص والنطاق المرجعي
5. أضف تعليقاً إن لزم
6. اضغط **حفظ النتائج**

#### حالات الطلب

| الحالة | المعنى |
|---|---|
| `PENDING` | في الانتظار |
| `IN_PROGRESS` | جارٍ تحليله |
| `COMPLETED` | النتائج جاهزة |
| `CANCELLED` | ملغى |

---

### 5.9 الأشعة

يعمل بنفس آلية المختبر:

1. **الطلب** — يُنشَأ من صفحة EMR أو مباشرة من صفحة الأشعة
2. **الإجراء** — يُسجَّل تاريخ الإجراء والجهاز المستخدم
3. **التقرير** — يُدخل الأخصائي الاستنتاجات والتوصيات
4. **النتيجة** — تظهر في EMR المريض تلقائياً

#### رفع صور الأشعة

يدعم النظام رفع صور الأشعة مباشرة إلى الطلب:

1. في صفحة **الأشعة**، اضغط **رفع الصور** في عمود الإجراءات
2. تظهر نافذة رفع الملفات تدعم:
   - **السحب والإفلات** (Drag & Drop)
   - **التصفح اليدوي** للملفات
3. الأنواع المدعومة: `JPEG`، `PNG`، `PDF`
4. الحجم الأقصى: **20 MB** لكل ملف
5. اضغط **رفع** — يتوفر شريط تقدم لكل ملف
6. يمكن عرض الصور المرفوعة وتنزيلها أو حذفها (حذف الصور للـ `SUPER_ADMIN` والـ `RADIOLOGIST` فقط)

> **التخزين:** تُحفظ الملفات على الخادم في `backend/uploads/radiology/{order_id}/` وتُسجَّل بياناتها في جدول `radiology_images`.

---

### 5.10 الموارد البشرية

#### تبويب الموظفين

**إضافة موظف:**
1. اضغط **+ موظف جديد**
2. أدخل:
   - الاسم والجنس وتاريخ الميلاد
   - القسم والمسمى الوظيفي
   - الراتب الأساسي
   - تاريخ التعيين
   - الفرع المنتسب إليه
   - الوردية (اختياري)
3. اضغط **حفظ**

**عرض ملف الموظف:**
- البيانات الأساسية
- سجل الحضور
- سجل الإجازات
- سجل الرواتب

#### تبويب الحضور

- **تسجيل الحضور:** تسجيل وقت الحضور والانصراف يدوياً
- **عرض الحضور الشهري:** تقرير يومي لكل موظف
- **تصفية:** حسب الموظف والتاريخ والحالة

#### تبويب الإجازات

**أنواع الإجازات:**
- سنوية، مرضية، طارئة، أمومة/أبوة، بدون راتب

**إدارة طلب الإجازة:**
1. اضغط **+ طلب إجازة**
2. اختر الموظف ونوع الإجازة
3. حدد تواريخ البداية والنهاية
4. أضف السبب
5. اضغط **تقديم**

**الموافقة أو الرفض:**
- اضغط على الطلب ← اختر **موافقة** أو **رفض**

#### تبويب الرواتب

1. اضغط **+ صرف رواتب**
2. اختر الشهر
3. راجع البيانات لكل موظف:
   - الراتب الأساسي
   - الإضافات
   - الخصومات
4. اضغط **تأكيد الصرف**

#### قسيمة الراتب (Payslip)

لكل سجل راتب يمكن استعراض قسيمة تفصيلية:

1. في تبويب **الرواتب**، اضغط **عرض القسيمة** بجانب أي سجل
2. تظهر نافذة تحتوي على:
   - **الراتب الأساسي**
   - **البدلات والمكافآت والأوفرتايم**
   - **أيام الغياب** — محسوبة من سجل الحضور الفعلي
   - **خصم الغياب** = (الراتب الأساسي ÷ 30) × عدد أيام الغياب
   - **الخصومات الأخرى**
   - **الضريبة**
   - **صافي الراتب**
3. اضغط **طباعة** لطباعة القسيمة

#### تبويب المناوبات (Shifts)

**إنشاء مناوبة:**
1. اضغط **+ مناوبة جديدة**
2. أدخل الاسم، وقت البداية، وقت النهاية
3. اختر أيام العمل (أحد، اثنين، إلخ)
4. اختر اللون للتمييز البصري
5. اضغط **حفظ**

**تعيين موظف لمناوبة:**
1. افتح المناوبة ← **إدارة التعيينات**
2. اختر الموظف وتاريخ بداية التعيين
3. اضغط **تعيين**

---

### 5.11 المحاسبة

#### نظرة مالية عامة

تعرض في أعلى الصفحة:
- إجمالي الإيرادات
- إجمالي المصروفات
- صافي الربح
- المصروفات هذا الشهر

#### تبويب الأصول

- **إضافة أصل:** معدات، أجهزة، مركبات، إلخ
- الحقول: الاسم، الفئة، قيمة الشراء، تاريخ الشراء، الاستهلاك السنوي

#### تبويب المصروفات

- **إضافة مصروف:** رواتب، إيجار، مستلزمات، خدمات
- الحقول: الوصف، المبلغ، الفئة، التاريخ، المورد (اختياري)

#### تبويب الموردون

- **إضافة مورد:** الاسم، الشركة، البريد، الهاتف، نوع الخدمة

#### تبويب أوامر الشراء

1. اضغط **+ أمر شراء**
2. اختر المورد
3. أضف البنود (الوصف، الكمية، سعر الوحدة)
4. حدد تاريخ التسليم المتوقع
5. اضغط **إنشاء**

**حالات الأمر:** مسودة، مُرسَل، مُستلَم جزئياً، مُستلَم كاملاً، ملغى

---

### 5.12 التقارير

تعرض صفحة التقارير إحصاءات تشمل:

**الإيرادات:**
- الإيرادات الشهرية (رسم بياني خطي)
- توزيع طرق الدفع (رسم بياني دائري)
- مقارنة شهر بشهر

**المرضى:**
- إجمالي المرضى المسجلين
- المرضى الجدد هذا الشهر
- المرضى حسب التصنيف العمري

**المواعيد:**
- معدل الحضور
- توزيع المواعيد على الأطباء
- أكثر أيام الأسبوع ازدحاماً

**الأداء:**
- أكثر الخدمات طلباً
- مقاييس أداء الأقسام

#### تصفية التقارير

يمكن تصفية جميع الإحصاءات حسب:
- **الفترة الزمنية:** آخر 7 أيام، 30 يوماً، 90 يوماً، سنة، أو نطاق مخصص
- **نطاق مخصص:** أدخل **تاريخ البداية** و**تاريخ النهاية** مباشرة
- **الطبيب:** تصفية الإيرادات والمواعيد لطبيب محدد
- **الفرع:** تصفية البيانات لفرع معين

#### تصدير التقارير

| الزر | الوظيفة |
|---|---|
| **تصدير Excel/CSV** | يُنزَّل ملف CSV (بـ UTF-8 BOM لدعم Excel) يحتوي على كل المدفوعات في النطاق الزمني المحدد |
| **طباعة / PDF** | يفتح مربع حوار الطباعة — يمكن حفظه كـ PDF من المتصفح |

---

### 5.13 إدارة المستخدمين

#### عرض المستخدمين

قائمة بجميع حسابات النظام مع:
- الاسم والبريد
- الأدوار المُعيَّنة
- تاريخ آخر دخول
- الحالة (نشط / غير نشط)

#### إضافة مستخدم جديد

1. اضغط **+ مستخدم جديد**
2. أدخل:
   - الاسم الكامل
   - البريد الإلكتروني (فريد في النظام)
   - كلمة المرور (يجب أن تستوفي معايير الأمان)
   - الأدوار (يمكن تعيين أكثر من دور)
3. اضغط **حفظ**

> **قاعدة أمنية:** لا يستطيع أي مستخدم رفع صلاحياته بنفسه (self-role-escalation محظور في الـ backend).

#### تعديل المستخدم

1. اضغط على المستخدم ← **تعديل**
2. يمكنك تغيير الاسم والأدوار والحالة
3. اضغط **حفظ**

#### تغيير كلمة المرور

1. افتح المستخدم ← **تغيير كلمة المرور**
2. أدخل كلمة المرور الجديدة (مرتين للتأكيد)
3. اضغط **تغيير**

#### تعطيل حساب

- اضغط على الحساب ← **تعطيل**
- المستخدم المُعطَّل لا يستطيع الدخول للنظام
- بياناته تبقى محفوظة ولا تُحذف

---

### 5.14 الإعدادات

#### تبويب عام

- **اسم العيادة**
- **العملة** (الرمز ورمز ISO)
- **المنطقة الزمنية** — تؤثر على عرض جميع التواريخ في النظام
- **ساعات العمل الافتراضية**

#### تبويب الفروع

**إضافة فرع:**
1. اضغط **+ إضافة فرع**
2. أدخل:
   - اسم الفرع
   - الرمز (كود فريد — مثال: `MAIN`, `BRANCH2`)
   - العنوان والهاتف والبريد
   - المنطقة الزمنية الخاصة بالفرع
   - المدير المسؤول
3. اضغط **حفظ**

**تعديل فرع:**
- اضغط على الفرع ← **تعديل**
- يمكن تغيير أي معلومة باستثناء الرمز (كود)

#### تبويب المناوبات

إدارة مناوبات العمل (الوصف الكامل في [قسم HR](#تبويب-المناوبات-shifts))

---

### 5.15 سجل التدقيق

#### عرض سجل التدقيق

- جدول بجميع العمليات التي نُفِّذت في النظام
- **العمود:** المستخدم، الإجراء، الوحدة، الكيان المتأثر، التاريخ والوقت

#### أنواع الإجراءات المُسجَّلة

| الإجراء | المعنى |
|---|---|
| `CREATE` | إنشاء سجل جديد |
| `UPDATE` | تعديل سجل |
| `DELETE` | حذف سجل |
| `LOGIN` | تسجيل دخول |
| `LOGOUT` | تسجيل خروج |
| `PAYMENT` | تسجيل دفعة |

#### التصفية

- **حسب المستخدم** — من نفّذ الإجراء
- **حسب الإجراء** — نوع العملية
- **حسب التاريخ** — من تاريخ إلى تاريخ
- **حسب الوحدة** — أي جزء من النظام

> سجل التدقيق **للقراءة فقط** — لا يمكن لأحد تعديله أو حذفه من الواجهة.

---

### 5.16 البحث الشامل

#### تفعيل البحث

يمكن فتح البحث الشامل بـ **ثلاث طرق:**
1. اضغط على **شريط البحث** في الشريط العلوي
2. اضغط مفتاح **`/`** من أي صفحة (بدون تركيز في حقل نصي)
3. اضغط **Alt + P** للانتقال إلى صفحة المرضى وابدأ البحث

#### كيفية الاستخدام

1. تظهر نافذة بحث شاملة تغطي الشاشة
2. ابدأ الكتابة — تظهر النتائج بعد 300ms تلقائياً
3. النتائج مجمّعة حسب النوع:
   - **المرضى** — الاسم، الرقم الطبي (MRN)، الهاتف
   - **المواعيد** — سبب الزيارة + اسم المريض + التاريخ
   - **الفواتير** — رقم الفاتورة + اسم المريض
   - **الأدوية** — الاسم التجاري والعلمي
   - **الموظفون** — الاسم والبريد (لأدوار HR فقط)
4. التنقل بين النتائج: مفاتيح **↑↓**، الفتح بـ **Enter**
5. الإغلاق: **Escape** أو النقر خارج النافذة

#### آخر عمليات البحث

تُحفظ آخر **5 عمليات بحث** في المتصفح وتظهر فوراً عند فتح البحث لتسريع الوصول.

---

### 5.17 اختصارات لوحة المفاتيح

#### عرض قائمة الاختصارات

اضغط **Alt + ?** من أي صفحة لعرض جميع الاختصارات المتاحة.

#### الاختصارات المتاحة

| الاختصار | الوظيفة | الفئة |
|---|---|---|
| `Alt + N` | موعد جديد — ينتقل إلى `/appointments/new` | إنشاء |
| `Alt + B` | فاتورة جديدة — ينتقل إلى `/billing` | إنشاء |
| `Alt + P` | البحث عن مريض — ينتقل إلى `/patients` | بحث |
| `Alt + H` | لوحة التحكم الرئيسية | تنقل |
| `Alt + L` | تسجيل الخروج فوراً | عام |
| `Alt + ?` | عرض/إخفاء نافذة الاختصارات | عام |
| `/` | فتح البحث الشامل | بحث |

> **ملاحظة:** الاختصارات لا تعمل عند التركيز داخل حقل نصي أو منطقة نصية.

---

### 5.18 مؤشر حالة الاتصال

في أسفل الشاشة يظهر **شريط أحمر** عند انقطاع الاتصال بالخادم، يحتوي على:
- رسالة "تعذّر الاتصال بالخادم"
- زر **إعادة المحاولة** للتحقق الفوري

**آلية العمل:**
- يُراقَب الاتصال بـ `/api/health` كل **60 ثانية**
- يستجيب لأحداث المتصفح `online` / `offline`
- يختفي الشريط تلقائياً عند عودة الاتصال

---

## 6. مرجع API الكامل

جميع الـ endpoints تتطلب كوكي `mediflow_token` صالحة ما لم يُذكر غير ذلك.

### المصادقة

| الطريقة | المسار | الوصف |
|---|---|---|
| `POST` | `/api/auth/login` | تسجيل الدخول (rate limit: 10/min) |
| `POST` | `/api/auth/logout` | تسجيل الخروج وإبطال الـ token |

**طلب تسجيل الدخول:**
```json
POST /api/auth/login
{
  "email": "admin@mediflow.com",
  "password": "Admin@1234"
}
```

**استجابة ناجحة:**
```json
{
  "id": "abc123...",
  "name": "Admin User",
  "email": "admin@mediflow.com",
  "roles": ["SUPER_ADMIN"]
}
```
*يُضبَط الكوكي `mediflow_token` تلقائياً في الاستجابة.*

---

### المرضى

| الطريقة | المسار | الوصف | الصلاحيات |
|---|---|---|---|
| `GET` | `/api/patients` | قائمة المرضى (مع pagination) | الكل |
| `POST` | `/api/patients` | إضافة مريض جديد | الكل |
| `GET` | `/api/patients/{id}` | تفاصيل مريض مع سجله | الكل |
| `PATCH` | `/api/patients/{id}` | تعديل بيانات مريض | الكل |
| `DELETE` | `/api/patients/{id}` | حذف ناعم للمريض | SUPER_ADMIN, CLINIC_MANAGER |

**Query Parameters لـ GET /api/patients:**
```
page=1            (الصفحة)
pageSize=10       (عدد النتائج)
search=           (بحث نصي)
status=ACTIVE|INACTIVE|ALL
```

---

### المواعيد

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/appointments` | قائمة المواعيد |
| `POST` | `/api/appointments` | موعد جديد |
| `GET` | `/api/appointments/{id}` | تفاصيل موعد |
| `PATCH` | `/api/appointments/{id}` | تعديل موعد |

---

### السجل الطبي (EMR)

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/emr/{patient_id}` | EMR كامل للمريض (مع كل السجلات) |
| `POST` | `/api/consultations` | إنشاء استشارة جديدة |

---

### الفواتير

| الطريقة | المسار | الوصف | الصلاحيات |
|---|---|---|---|
| `GET` | `/api/billing/stats` | إحصاءات الفوترة | ACCOUNTANT, RECEPTIONIST, SUPER_ADMIN, CLINIC_MANAGER |
| `GET` | `/api/billing` | قائمة الفواتير | نفس الأعلى |
| `POST` | `/api/billing` | إنشاء فاتورة | نفس الأعلى |
| `GET` | `/api/billing/{id}` | تفاصيل فاتورة | نفس الأعلى |
| `PATCH` | `/api/billing/{id}` | تعديل فاتورة | نفس الأعلى |
| `POST` | `/api/billing/{id}/payments` | تسجيل دفعة | أي مستخدم مُصادَق |

**طلب إنشاء فاتورة:**
```json
POST /api/billing
{
  "patientId": "abc123...",
  "appointmentId": "xyz789...",
  "items": [
    {
      "description": "استشارة طبيب عام",
      "category": "CONSULTATION",
      "quantity": 1,
      "unitPrice": 50.0,
      "discount": 0
    }
  ],
  "taxRate": 16,
  "insuranceClaim": false
}
```

---

### الصيدلية

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/pharmacy/medications` | قائمة الأدوية |
| `POST` | `/api/pharmacy/medications` | إضافة دواء |
| `PATCH` | `/api/pharmacy/medications/{id}` | تعديل دواء |
| `GET` | `/api/pharmacy/stock-movements` | حركات المخزون |
| `POST` | `/api/pharmacy/stock-movements` | تسجيل حركة مخزون |

---

### الموارد البشرية

| الطريقة | المسار | الوصف | الصلاحيات |
|---|---|---|---|
| `GET` | `/api/hr/employees` | قائمة الموظفين | HR_OFFICER, SUPER_ADMIN, CLINIC_MANAGER |
| `POST` | `/api/hr/employees` | إضافة موظف | نفس الأعلى |
| `PATCH` | `/api/hr/employees/{id}` | تعديل موظف | نفس الأعلى |
| `DELETE` | `/api/hr/employees/{id}` | حذف ناعم | نفس الأعلى |
| `GET` | `/api/hr/leaves` | قائمة طلبات الإجازة | نفس الأعلى |
| `POST` | `/api/hr/leaves` | طلب إجازة | نفس الأعلى |
| `PATCH` | `/api/hr/leaves/{id}` | قبول/رفض إجازة | نفس الأعلى |
| `GET` | `/api/hr/attendance` | سجل الحضور | نفس الأعلى |
| `POST` | `/api/hr/attendance` | تسجيل حضور | نفس الأعلى |
| `GET` | `/api/hr/payroll` | سجل الرواتب | نفس الأعلى |
| `POST` | `/api/hr/payroll` | صرف رواتب | نفس الأعلى |

---

### المحاسبة

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/accounting/overview` | النظرة المالية العامة |
| `GET/POST` | `/api/accounting/assets` | الأصول |
| `GET/POST` | `/api/accounting/expenses` | المصروفات |
| `GET/POST` | `/api/accounting/vendors` | الموردون |
| `GET/POST` | `/api/accounting/purchase-orders` | أوامر الشراء |

---

### المختبر والأشعة

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET/POST` | `/api/laboratory` | طلبات المختبر |
| `PATCH` | `/api/laboratory/{id}` | تحديث نتائج |
| `GET/POST` | `/api/radiology` | طلبات الأشعة |
| `PATCH` | `/api/radiology/{id}` | تحديث تقرير الأشعة |
| `POST` | `/api/radiology/{id}/images` | رفع صورة أشعة (multipart/form-data) |
| `GET` | `/api/radiology/{id}/images` | قائمة صور الطلب |
| `GET` | `/api/radiology/images/{image_id}` | تنزيل صورة |
| `DELETE` | `/api/radiology/images/{image_id}` | حذف صورة (SUPER_ADMIN, RADIOLOGIST) |

**رفع صورة (مثال):**
```
POST /api/radiology/{order_id}/images
Content-Type: multipart/form-data

file: <binary> (jpeg/png/pdf, max 20MB)
```

---

### التقارير ولوحة التحكم

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/dashboard/stats` | إحصاءات لوحة التحكم |
| `GET` | `/api/reports/overview` | تقرير شامل |
| `GET` | `/api/reports/export` | تصدير CSV للمدفوعات |
| `GET` | `/api/audit` | سجل التدقيق |

**Query Parameters لـ GET /api/reports/overview:**
```
range=7d|30d|90d|365d    (الفترة الزمنية — افتراضي: 30d)
date_from=YYYY-MM-DD     (بداية نطاق مخصص — يُلغي range)
date_to=YYYY-MM-DD       (نهاية نطاق مخصص)
doctor_id=               (تصفية بالطبيب)
branch_id=               (تصفية بالفرع)
```

**Query Parameters لـ GET /api/reports/export:**
```
format=csv               (النوع — افتراضي: csv)
date_from=YYYY-MM-DD
date_to=YYYY-MM-DD
```
*يُعيد ملف CSV بـ UTF-8 BOM.*

---

### البحث الشامل

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/search` | بحث موحّد عبر كل الوحدات |

**Query Parameters:**
```
q=<نص البحث>      (3 أحرف على الأقل)
limit=10           (عدد النتائج — افتراضي: 10)
```

**مثال استجابة:**
```json
[
  { "type": "patient",     "id": "abc123", "title": "أحمد محمد", "subtitle": "MRN: 001234", "url": "/patients/abc123" },
  { "type": "appointment", "id": "xyz789", "title": "زيارة دورية", "subtitle": "أحمد محمد — 2026-05-07", "url": "/appointments/xyz789" },
  { "type": "invoice",     "id": "inv001", "title": "INV-2026-001", "subtitle": "أحمد محمد", "url": "/billing/inv001" }
]
```

---

### التحديثات الفورية (SSE)

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/events/dashboard` | بث إحصاءات لوحة التحكم كل 5 ثوانٍ |
| `GET` | `/api/events/appointments` | بث تحديثات المواعيد |
| `GET` | `/api/events/lab` | بث تحديثات المختبر |

**الاستخدام في Frontend:**
```typescript
const source = new EventSource("/api/events/dashboard", { withCredentials: true });
source.onmessage = (e) => {
  const data = JSON.parse(e.data);
  // { todayAppointments, activeConsultations, pendingLab }
};
```

---

### الموارد البشرية — إضافات

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/hr/payroll/{id}/payslip` | قسيمة راتب تفصيلية مع خصم الغياب |
| `GET` | `/api/hr/attendance/summary/{employee_id}` | ملخص حضور شهري |

**Query Parameters لـ GET /api/hr/attendance/summary/{id}:**
```
month=YYYY-MM    (الشهر — افتراضي: الشهر الحالي)
```

**مثال استجابة payslip:**
```json
{
  "employeeName": "خالد العمري",
  "month": "2026-05",
  "basicSalary": 3000,
  "allowances": 500,
  "bonus": 0,
  "overtimePay": 150,
  "absenceDays": 2,
  "absenceDeduction": 200,
  "otherDeductions": 0,
  "tax": 100,
  "grossSalary": 3650,
  "netSalary": 3350
}
```

---

### المستخدمون والإعدادات

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/users` | قائمة المستخدمين |
| `POST` | `/api/users` | إضافة مستخدم |
| `PATCH` | `/api/users/{id}` | تعديل مستخدم |
| `DELETE` | `/api/users/{id}` | حذف مستخدم |
| `GET` | `/api/settings` | قراءة إعدادات النظام |
| `PATCH` | `/api/settings` | تعديل الإعدادات |
| `GET/POST` | `/api/branches` | إدارة الفروع |
| `PATCH` | `/api/branches/{id}` | تعديل فرع |
| `GET/POST` | `/api/shifts` | إدارة المناوبات |
| `PATCH/DELETE` | `/api/shifts/{id}` | تعديل/حذف مناوبة |
| `GET` | `/api/shifts/{id}/assignments` | تعيينات المناوبة |
| `POST/DELETE` | `/api/shifts/assignments` | إدارة التعيينات |
| `GET` | `/api/doctors` | قائمة الأطباء |

---

### رموز الاستجابة

| الكود | المعنى |
|---|---|
| `200` | ناجح |
| `201` | تم الإنشاء |
| `400` | طلب غير صالح |
| `401` | غير مُصادَق — token مفقود أو منتهي |
| `403` | محظور — لا تملك الصلاحية |
| `404` | السجل غير موجود |
| `409` | تعارض — البيانات موجودة مسبقاً |
| `422` | خطأ في التحقق من البيانات |
| `429` | تجاوزت حد الطلبات (rate limit) |
| `500` | خطأ داخلي في الخادم |

---

## 7. الأمان والحماية

### طبقات الحماية

```
┌─ 1. Rate Limiting (slowapi) ──────────────────────────┐
│   200 طلب/دقيقة (عام) | 10 طلبات/دقيقة (تسجيل الدخول) │
├─ 2. Edge JWT (middleware.ts) ─────────────────────────┤
│   يتحقق من الكوكي قبل عرض أي صفحة                   │
├─ 3. RBAC (require_roles) ─────────────────────────────┤
│   كل endpoint يتحقق من دور المستخدم                   │
├─ 4. Token Blocklist ──────────────────────────────────┤
│   كل طلب يتحقق من أن الـ token لم يُبطَل              │
├─ 5. Input Validation (Pydantic) ──────────────────────┤
│   التحقق من صحة البيانات قبل حفظها                   │
├─ 6. XSS Sanitization ─────────────────────────────────┤
│   تنظيف المدخلات النصية من HTML خطرة                  │
├─ 7. Security Headers (next.config.ts) ────────────────┤
│   CSP, X-Frame-Options, X-Content-Type-Options         │
├─ 8. File Upload Validation ───────────────────────────┤
│   التحقق من نوع الملف والحجم (≤20MB, jpeg/png/pdf)    │
└───────────────────────────────────────────────────────┘
```

### إعدادات الأمان المُفعَّلة

| الميزة | الإعداد |
|---|---|
| تشفير كلمة المرور | bcrypt, 12 rounds |
| مدة الـ JWT | 8 ساعات |
| الكوكي | HttpOnly + SameSite=Strict |
| COOKIE_SECURE | `false` محلياً (HTTP) — `true` في الإنتاج (HTTPS إلزامي) |
| CSRF Protection | Double-submit cookie على جميع طلبات POST/PATCH/DELETE |
| قفل الجلسة بالخمول | 25 دقيقة تحذير + 5 دقائق logout |
| Rate limit — تسجيل دخول | 10 محاولات/دقيقة لكل IP |
| Rate limit — عام | 200 طلب/دقيقة |
| CORS | مُقيَّد بـ `ALLOWED_ORIGINS` |
| Token Blocklist | SHA-256 hash في قاعدة البيانات |

### قواعد كلمة المرور

كلمة المرور يجب أن تستوفي:
- 8 أحرف على الأقل
- حرف كبير واحد على الأقل
- حرف صغير واحد على الأقل
- رقم واحد على الأقل
- رمز خاص واحد على الأقل

### ما يجب فعله في بيئة الإنتاج

> **إذا استخدمت `deploy.sh`** فالبنود التلقائية ✅ تُنجز وحدها — تحتاج فقط ما هو يدوي ✋.

- ✅ `JWT_SECRET` و`SETTINGS_ENCRYPTION_KEY` تُولَّد تلقائياً بـ `deploy.sh`
- ✅ مستخدم MySQL مخصص (`mediflow_app`) يُنشأ بكلمة مرور عشوائية تلقائياً
- ✅ `COOKIE_SECURE` يُضبط تلقائياً (`false` للـ IP، `true` للنطاق)
- ✅ `ALLOWED_ORIGINS` يُضبط تلقائياً بالنطاق أو IP الذي أدخلته
- ✋ فعّل HTTPS بعد النشر: `sudo certbot --nginx -d yourdomain.com`
- ✋ غيّر كلمة مرور admin إذا نسيتها أو أردت تغييرها لاحقاً
- ✋ لا ترفع `.env` أو `.env.local` على Git (مضافان في `.gitignore`)

---

## 8. إدارة قاعدة البيانات

### هيكل الجداول الرئيسية

```
patients ──────────────────────────────────────────────┐
  id, mrn, firstName, lastName, dateOfBirth, gender     │
  phone, email, bloodType, allergies (JSON)              │
  chronicConditions (JSON), insuranceProvider            │
  isActive, deletedAt (soft delete)                     │
                                                        ▼
appointments ──────────────────────── consultations ─────
  id, patientId, doctorId, scheduledAt    id, patientId, doctorId
  status, type, reason, branchId          chiefComplaint, assessment
                                          plan, isLocked

invoices ──────────────────────────── invoice_items ─────
  id, invoiceNo, patientId, appointmentId   id, invoiceId
  subtotal, discountAmount, taxAmount       description, quantity
  totalAmount, paidAmount, balance          unitPrice, totalPrice
  status, insuranceClaim

payments ──────────────────────────────────────────────
  id, invoiceId, amount, method, referenceNo, paidAt

medications ──────────────── stock_movements ────────────
  id, name, genericName         id, medicationId, type
  unit, quantity, minQuantity   quantity, reason, userId
  unitPrice, expiryDate

employees ─────────────────────────────────────────────
  id, userId, department, jobTitle
  baseSalary, hireDate, branchId, deletedAt

shifts ──────────────── shift_assignments ────────────
  id, name, startTime       id, employeeId, shiftId
  endTime, daysOfWeek       startDate, endDate
  color, isActive, branchId

users ──────────────────────────────────────────────────
  id, name, email, passwordHash, roles (JSON), isActive

token_blocklist ────────────────────────────────────────
  id, tokenHash (SHA-256), expiresAt

audit_logs ─────────────────────────────────────────────
  id, userId, action, module, entityId, entityType, createdAt
```

### Migrations — Alembic

الـ migrations تتتبع التغييرات في `models.py` وتطبّقها على قاعدة البيانات بشكل آمن.

```bash
cd backend

# إنشاء migration جديد بعد تعديل models.py
alembic revision --autogenerate -m "وصف التعديل"

# تطبيق جميع الـ migrations المعلّقة
alembic upgrade head

# عرض الحالة الحالية
alembic current

# التراجع عن آخر migration
alembic downgrade -1
```

> **إنتاج:** `deploy.sh` و `update.sh` يُشغّلان `alembic upgrade head` تلقائياً.
> **تطوير:** شغّل `alembic upgrade head` يدوياً بعد أي تغيير في `models.py`.

### النسخ الاحتياطي

```bash
# نسخة احتياطية كاملة
mysqldump -u mediflow_user -p mediflow > backup_$(date +%Y%m%d).sql

# استعادة من نسخة احتياطية
mysql -u mediflow_user -p mediflow < backup_20260507.sql
```

### فهارس قاعدة البيانات (للأداء)

الفهارس مضافة تلقائياً على الأعمدة عالية الاستخدام:
- `Consultation.patientId`, `Consultation.doctorId`, `Consultation.createdAt`
- `Payment.invoiceId`
- `StockMovement.medicationId`
- `Employee.branchId`, `Employee.deletedAt`
- `AuditLog.userId`, `AuditLog.createdAt`

---

## 9. استكشاف الأخطاء وحلها

### المشكلة: الصفحة تعيد التوجيه إلى `/login` باستمرار

**الأسباب المحتملة:**
1. **انتهاء صلاحية الـ JWT** — تحقق من أن ساعة السيرفر صحيحة
2. **JWT_SECRET مختلف** بين Backend وFrontend
3. **الكوكي محذوف** من المتصفح

**الحل:**
```bash
# تحقق من JWT_SECRET في كلا الملفين
cat backend/.env | grep JWT_SECRET
cat .env.local | grep JWT_SECRET
# يجب أن تكون القيمتان متطابقتين تماماً
```

---

### المشكلة: خطأ 500 عند تسجيل الدخول

**الأسباب المحتملة:**
1. **قاعدة البيانات لا تعمل**
2. **DATABASE_URL خاطئ**
3. **المستخدم غير موجود في قاعدة البيانات**

**الحل:**
```cmd
# تحقق من تشغيل MySQL
cd backend
python -c "from database import engine; print('DB OK')"

# أعِد إنشاء المستخدم الافتراضي
python seed.py
```

---

### المشكلة: `npm run dev` يفشل مع خطأ Tailwind

**السبب:** Turbopack لا يدعم Tailwind عندما يحتوي مسار المشروع على مسافات.

**الحل:** استخدم `npm run dev` (يستخدم webpack تلقائياً) ولا تستخدم `next dev --turbo`.

---

### المشكلة: الـ Backend يعمل لكن الـ Frontend لا يتصل به

**الحل:**
```bash
# تحقق من أن Backend يعمل على المنفذ الصحيح
curl http://localhost:8000/api/dashboard/stats

# تحقق من إعداد الـ proxy في next.config.ts
# يجب أن يكون BACKEND_URL صحيحاً في .env.local
```

---

### المشكلة: رسالة "Too Many Requests" (429)

**السبب:** تجاوزت حد الطلبات المسموح به.

**الحل:**
- انتظر دقيقة واحدة
- تحقق من أن لديك طلبات حلقية غير مقصودة في الكود

---

### المشكلة: خطأ في قاعدة البيانات — "Table doesn't exist"

**الحل:**
```cmd
cd backend
python reset_db.py --confirm
```
> **تحذير:** يحذف جميع البيانات. استخدمه فقط في التطوير.

---

### المشكلة: فشل تطبيق Alembic migration

**الأسباب المحتملة:**
1. `DATABASE_URL` خاطئ في `backend/.env`
2. تعارض في بنية الجداول (يتطلب `alembic downgrade`)
3. لا يوجد ملف `alembic.ini` أو `migrations/env.py`

**الحل:**
```bash
cd /opt/mediflow/backend
source .venv/bin/activate   # Linux

# تحقق من الحالة الحالية
alembic current

# أعد تطبيق الـ migration
alembic upgrade head

# إذا كان هناك تعارض — تراجع ثم أعد
alembic downgrade -1
alembic upgrade head
```

---

### المشكلة: فشل update.sh أو deploy.sh

**الحل:**
```bash
# اعرض الخطأ بالتفصيل
sudo bash /opt/mediflow/deploy/update.sh 2>&1 | tee /tmp/update.log
cat /tmp/update.log

# تحقق من logs الخدمات
sudo journalctl -u mediflow-backend -n 50
sudo journalctl -u mediflow-frontend -n 50
```

---

### إيقاف وإعادة تشغيل الخدمات على Linux

```bash
# إعادة تشغيل Backend
sudo systemctl restart mediflow-backend

# إعادة تشغيل Frontend
sudo systemctl restart mediflow-frontend

# عرض آخر السجلات
sudo journalctl -u mediflow-backend -n 50
sudo journalctl -u mediflow-frontend -n 50

# حالة جميع الخدمات
sudo systemctl status mediflow-backend mediflow-frontend nginx mysql
```

---

## 10. مرجع سريع للمشرف

### قائمة تحقق اليومية

- [ ] تحقق من حالة الخدمات: `systemctl status mediflow-backend mediflow-frontend`
- [ ] راجع سجل التدقيق لأي نشاط غير اعتيادي
- [ ] تحقق من تنبيهات المخزون المنخفض في لوحة التحكم
- [ ] تحقق من الفواتير المتأخرة

### قائمة تحقق الإعداد الأولي

- [ ] تغيير كلمة مرور admin الافتراضية
- [ ] إضافة بيانات العيادة في الإعدادات
- [ ] إنشاء الفروع (إن وجدت)
- [ ] إنشاء حسابات المستخدمين وتعيين أدوارهم
- [ ] إعداد المناوبات وتعيين الموظفين لها
- [ ] إضافة الأطباء وإنشاء حساباتهم

### أوامر الطوارئ

```cmd
# إيقاف كل شيء (Windows)
stop.bat

# إعادة تشغيل كل شيء (Windows)
start.bat
```

```bash
# تحديث النظام كاملاً (Ubuntu — بعد كل push)
sudo bash /opt/mediflow/deploy/update.sh

# إعادة تشغيل خدمة واحدة (Ubuntu)
sudo systemctl restart mediflow-backend
sudo systemctl restart mediflow-frontend
```

```cmd
# أوامر قاعدة البيانات

# إعادة تعيين كلمة مرور admin (يتطلب وصولاً لقاعدة البيانات)
cd backend
python -c "
from database import SessionLocal
from auth import hash_password
import models
db = SessionLocal()
user = db.query(models.User).filter(models.User.email == 'admin@mediflow.com').first()
user.passwordHash = hash_password('NewPassword@1234')
db.commit()
print('Password reset successfully')
db.close()
"
```

### معلومات الاتصال الافتراضية

| الخدمة | المنفذ | الرابط |
|---|---|---|
| Frontend (Next.js) | 3000 | `http://localhost:3000` |
| Backend (FastAPI) | 8000 | `http://localhost:8000` |
| API Docs (Swagger) | 8000 | `http://localhost:8000/docs` |
| MySQL | 3306 | `localhost:3306/mediflow` |

> **Swagger UI:** في بيئة التطوير، يتوفر التوثيق التفاعلي للـ API على `http://localhost:8000/docs` — يمكنك تجربة الـ endpoints مباشرة.

---

---

## 11. ملاحظات التحديث — v2.0

### ميزات مضافة في v2.0

| الميزة | الوصف |
|---|---|
| **البحث الشامل** | بحث موحّد عبر المرضى، المواعيد، الفواتير، الأدوية، الموظفين — مع تنقل بلوحة المفاتيح وسجل البحث |
| **اختصارات لوحة المفاتيح** | 6 اختصارات `Alt+Key` + فتح البحث بـ `/` |
| **مؤشر حالة الاتصال** | شريط تحذير عند انقطاع الخادم مع مراقبة تلقائية |
| **تحديثات فورية (SSE)** | لوحة التحكم تُحدَّث مباشرةً عبر Server-Sent Events |
| **الإجراءات السريعة للمواعيد** | فتح EMR، استشارة سريعة، وإصدار فاتورة بنقرة واحدة |
| **رفع صور الأشعة** | drag-and-drop لرفع JPEG/PNG/PDF مع حفظ على الخادم |
| **قسيمة الراتب التفصيلية** | حساب خصم الغياب من سجل الحضور الفعلي + طباعة |
| **تصدير التقارير** | CSV بـ UTF-8 BOM + طباعة PDF |
| **تصفية التقارير** | نطاق تاريخ مخصص + فلتر طبيب + فلتر فرع |
| **إعادة المحاولة التلقائية** | `apiFetchWithRetry` مع تأخيرات 1s، 2s، 4s |
| **إمكانية الوصول (a11y)** | `useFocusTrap`، `useReturnFocus`، `useAnnounce` لدعم قارئات الشاشة |

*آخر تحديث: مايو 2026 — MediFlow CMS v2.0*
