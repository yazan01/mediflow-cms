# ── Next.js Frontend ─────────────────────────────────────────────────────────
# Internal URL of the FastAPI backend (server-side only, not exposed to browser)
BACKEND_URL=http://localhost:8000

# JWT secret — must match backend JWT_SECRET exactly
JWT_SECRET=REPLACE_WITH_SAME_SECRET_AS_BACKEND
